# Base URL of the dome-agent-flow shim API (no trailing slash). Host-aware: on
# localhost the app falls back to http://localhost:8000 when this is unset.
NEXT_PUBLIC_AGENTFLOW_API_BASE=

# Shown by the dome-ui StagingBanner ("staging"/"production"); label only.
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development
